# Local configuration - holds the database password. Never commit these.
# The committed templates are config.local.example.php and .env.example.
config.local.php
.env
.env.*
!.env.example

# Uploaded content
/User Directories/*/

# Keep the deny rules that protect the storage directory
!/User Directories/.htaccess
!/User Directories/web.config

test1.txt
